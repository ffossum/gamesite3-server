{
  "name": "gamesite3-server",
  "version": "0.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "prettier": "prettier --write --single-quote --trailing-comma es5 *.js src/**/*.js",
    "test": "flow check && prettier --list-different --single-quote --trailing-comma es5 *.js src/**/*.js"
  },
  "dependencies": {
    "koa": "^2.2.0",
    "koa-router": "^7.2.0"
  },
  "devDependencies": {
    "flow-bin": "^0.46.0",
    "prettier": "^1.3.1"
  }
}
